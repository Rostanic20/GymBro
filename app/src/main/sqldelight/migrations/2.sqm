ALTER TABLE food ADD COLUMN is_favourite INTEGER NOT NULL DEFAULT 0;

CREATE TABLE recipe (
    id   INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT    NOT NULL
);

CREATE TABLE recipe_item (
    recipe_id INTEGER NOT NULL REFERENCES recipe(id),
    food_id   INTEGER NOT NULL REFERENCES food(id),
    grams     REAL    NOT NULL,
    PRIMARY KEY (recipe_id, food_id)
);

CREATE TABLE food_log (
    id             INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    date_epoch_day INTEGER NOT NULL,
    meal_slot      INTEGER NOT NULL,
    food_id        INTEGER REFERENCES food(id),
    name           TEXT    NOT NULL,
    grams          REAL,
    kcal           REAL    NOT NULL,
    protein_g      REAL    NOT NULL,
    carbs_g        REAL    NOT NULL,
    fat_g          REAL    NOT NULL,
    logged_at      INTEGER NOT NULL
);

CREATE INDEX food_log_date ON food_log(date_epoch_day, meal_slot);

CREATE TABLE body_weight (
    date_epoch_day INTEGER NOT NULL PRIMARY KEY,
    weight_kg      REAL    NOT NULL
);

INSERT INTO recipe(id, name) VALUES
    (1, 'Shake #1'),
    (2, 'Shake #2'),
    (3, 'Shake #1, no whey'),
    (4, 'Shake #2, no whey'),
    (5, 'Dinner: eggs and toast'),
    (6, 'Pre-gym: bread, honey, banana'),
    (7, 'Desk snack: walnuts and dates');

INSERT INTO recipe_item(recipe_id, food_id, grams) VALUES
    (1, 1, 250), (1, 2, 40), (1, 3, 120), (1, 4, 25),
    (2, 1, 300), (2, 2, 50), (2, 3, 120), (2, 5, 20), (2, 4, 25),
    (3, 1, 300), (3, 2, 40), (3, 3, 120), (3, 6, 150),
    (4, 1, 300), (4, 2, 50), (4, 3, 120), (4, 5, 20), (4, 6, 150),
    (5, 7, 165), (5, 8, 60), (5, 12, 5),
    (6, 8, 60), (6, 9, 14), (6, 3, 120),
    (7, 10, 20), (7, 11, 30);
